[build]
  command = "pip install -r requirements.txt"
  publish = "."

[functions]
  directory = "netlify/functions"
  included_files = ["data/**", "nlp_processor.py", "app.py"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200
